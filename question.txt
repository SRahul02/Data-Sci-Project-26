Word Embedding based search engine. Build word embeddings, use near neighbor based search to find out related terms and then return documents. Evaluate the search engine and see how it performs. For word embeddings you can use Word2Vec or BERT. For retrieval of documents based on a query, use approximate near neighbor search algorithms. Use standard search metrics e.g. precision@k and recall @ k where k is the number of documents that you are returning.